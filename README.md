# HyperFaaS

Architecture Overview can be found in our tubcloud codes folder

to run the docker compose without the leaf:
```
docker compose up -d --build --no-deps worker
```

to run the docker compose only the leaf:
```
docker compose up -d --build --no-deps leaf
```

