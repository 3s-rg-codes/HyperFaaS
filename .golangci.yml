version: "2"
run:
  allow-parallel-runners: true
formatters:
  enable:
    - gofmt
    - gofumpt
    - goimports

linters:
  exclusions:
    paths:
      - functions
  enable:
    - errcheck
    - asciicheck
    - bidichk
    - nilerr
    - perfsprint
    - unconvert
    - whitespace